# Study App Backend

FastAPI + PostgreSQL backend scaffold for the Study App project.

## Current State

- Async FastAPI app skeleton
- Shared settings and database wiring
- Base ORM model and timestamp mixin
- Route placeholders for the documented API groups

## Next Build Step

Implement the `users` and `folders` models first, then add Alembic migrations and repository/service layers on top of the shared database wiring.
